Round avatar, photo or initials-on-ink fallback. Add `ring` to flag items needing attention (e.g. a référent's queue).

```jsx
<Avatar initials="JD" />
<Avatar src="/assets/photo.jpg" size="lg" ring />
```
