A square, rounded-12 glyph button on a muted cream fill — used for the sidebar collapse toggle and the "open competency" row action.

```jsx
<IconButton aria-label="Ouvrir">
  <ChevronIcon />
</IconButton>
```

Variants: default (muted cream background) and `active` (dark ink fill, e.g. selected sidebar toggle).
